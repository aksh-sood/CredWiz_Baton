eureka:
  client:
    service-url:
      defaultZone: http://eureka-server:8761/eureka
    register-with-eureka: true
    fetch-registry: true

server:
  port: 4000

spring:
  application:
    name: "product-webapp"

